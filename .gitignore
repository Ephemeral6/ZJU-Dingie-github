# ================= 魔搭创空间专用 .gitignore =================

# macOS system files
.DS_Store

# Python files
__pycache__/
*.pyc
lab.py
.idea

# Virtual environment (魔搭会自动创建)
.venv
.conda
conda

# 敏感数据 (API密钥等 - 魔搭使用环境变量)
.env
api_keys.py
src/open_llm_vtuber/llm/user_credentials.json

# 日志文件
server.log
logs/*

# 缓存文件 (运行时生成)
cache/*
src/open_llm_vtuber/tts/asset
src/open_llm_vtuber/tts/config
asset

# 临时文件
tmp/
private/

# IDE 配置
.vscode/

# 其他
*.exe
mem.json
conf.yaml.backup
openapi_assistants.json
openapi_memgpt.json
legacy/
submodules/MeloTTS

# ===== 大文件 (魔搭运行时下载) =====
# 模型文件 (太大无法上传 GitHub，魔搭运行时下载，使用 HF 镜像加速)
models/
# 向量数据库 (运行时构建)
chroma_db/
# 聊天记录 (运行时生成)
chat_history/

# ===== 以下文件需要上传到魔搭 =====
# conf.yaml - 需要上传 (配置文件)
# knowledge_data/ - 需要上传 (知识库数据)
# live2d-models/ - 需要上传 (Live2D模型)
# frontend/ - 需要上传 (前端文件)
# backgrounds/ - 需要上传 (背景图片)
# avatars/ - 需要上传 (头像)
# characters/ - 需要上传 (角色配置)
# scripts/ - 需要上传 (构建脚本)
